#include <context_manager.h>
#include "engine.h"

void process_message(struct message_state *ms, safetable_t *table)
{
	return;
	//int err;

	//if (should_manage_ctx(ms)) {
	//	err = manage_process_context(table, ms);
	//	transition_progress_state(ms);
	//}

	// TODO: save message into database
	// TODO: transition message state
}
